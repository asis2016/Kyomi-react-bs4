interface CardFeatureProps{
    /**
     * Interface for card feature as of v.1.0
     * */
    /* todo href and hrefTitle should be a button (atom) */
    id: number;
    href: string;
    hrefTitle:string;
    title:string;
    image: string;
}